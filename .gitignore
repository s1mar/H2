# Android / Gradle build output (match anywhere, not just root)
.gradle/
build/
app/build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.ap_
*.dex
*.hprof
local.properties
# IDE
.idea/
*.iml
.DS_Store

# google-services.json holds a Firebase API key — NEVER commit it to a public repo.
# Keep your downloaded copy at app/google-services.json locally (untracked).
app/google-services.json
